{
  "id": "content-generator",
  "version": "1.1.0",
  "requiredVariables": ["topic", "tone", "maxLength"],
  "template": "Write a short paragraph about {{topic}} in a {{tone}} tone. Ensure it stays under {{maxLength}} characters."
}
